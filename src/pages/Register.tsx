import { CreateTransaction } from '@/components/queue/CreateTransaction';

const Register = () => {
  return (
    <div className="container mx-auto px-4 py-8 min-h-[calc(100vh-4rem)] flex items-center justify-center">
      <CreateTransaction />
    </div>
  );
};

export default Register;
